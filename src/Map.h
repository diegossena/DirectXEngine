#include <string>
#include <vector>
#include "Engine/Utils/Size.h"

class Map
{
private:
  std::vector<std::string> oGameMap;
  int oWidth, oHeight;
  float oTileSize;

public:
  // Getters
  std::vector<std::string> &current = Map::oGameMap;
  const float &tileSize = Map::oTileSize;
  // Constructor
  Map(float tileSize_) : oTileSize(tileSize_) {}
  // Methods
  void loadLvL1()
  {
    Map::oGameMap = {
      "................................................................",
      "................................................................",
      ".......ooooo....................................................",
      ".......ooooo....................................................",
      "........ooo.....................................................",
      ".......................########.................................",
      ".....BB?BBBB?BB.......###..............#.#......................",
      "....................###................#.#......................",
      "...................####.........................................",
      "GGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGGG.##############.....########",
      "...................................#.#...............###........",
      "........................############.#............###...........",
      "........................#............#.........###..............",
      "........................#.############......###.................",
      "........................#................###....................",
      "........................#################.......................",
      "................................................................"
    };
  }
  char getTile(const unsigned short &x, const unsigned short int &y)
  {
    if (x >= 0 && x < Map::oGameMap[0].size() && y >= 0 && y < Map::oGameMap.size())
      return Map::oGameMap[y][x];
    return ' ';
  }
};